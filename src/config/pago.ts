import type { ConferenciaKey } from "@/config/transferencia";

/** Textos solo de UI (sin montos secretos). */
export const PAGO_PRODUCTO_LABEL: Record<ConferenciaKey, string> = {
  brave: "Conferencia Brave",
  valiente: "Conferencia Valiente",
};
